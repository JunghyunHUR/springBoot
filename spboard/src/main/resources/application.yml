#server setting
server:
    port: 9000

#database setting
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/spbbs?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
        username: root
        password: ss2414ss
    thymeleaf:
        cache: false

    #jpa setting
    jpa:
        #database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        open-in-view: false
        show-sql: true
        hibernate:
            ddl_auto: update